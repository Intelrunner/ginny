# Exclude version control directories
.git
.gitignore

# Exclude Go cache and modules cache
*.log
*.out
*.test
*.swp

# Exclude output binaries (in case they are in the project folder)
bin/
*.exe
*.dll
*.so
*.dylib

# Exclude editor-specific files and directories
*.idea/
*.vscode/
*.sublime-project
*.sublime-workspace

# Exclude temporary files
*.tmp
*.bak
*.swp
*.DS_Store
Thumbs.db

# Exclude build directories (if any)
dist/
build/

# Exclude config files that should not be part of the image
*.env
config.yaml
config.json

# Keep only necessary application files
# (e.g., main application source, go.mod, go.sum)
!main.go
!go.mod
!go.sum
